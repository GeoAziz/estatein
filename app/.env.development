# Committed intentionally on the Demo branch only — no secrets here, just a
# public build flag. Makes `npm run dev` use the backend-free demo mode
# (src/lib/demo/) automatically. See app/.env.production for the build/deploy
# equivalent; neither affects vitest ("test" mode), so unit tests are unaffected.
VITE_DEMO_MODE=true
